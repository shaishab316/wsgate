{
  "name": "wsgate-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:ui": "pnpm --filter @wsgate/ui dev",
    "dev:example": "pnpm --filter simple-chat-app dev"
  }
}

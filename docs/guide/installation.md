# Installation

## Requirements

- NestJS 10 or 11
- Node.js 18+

## Install

```bash
pnpm add @wsgate/nest
```

`@wsgate/ui` is a peer dependency — it is installed automatically with `@wsgate/nest`.
